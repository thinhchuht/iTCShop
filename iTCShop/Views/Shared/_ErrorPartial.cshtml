@model ResponseModel
<script>
    @{
        
        if (Model != null)
        {
            var message = @Model.Message.Replace("\n", "\\n").Replace("\r", "");
            <text>alert("@message")</text>
        }
    }
</script>
